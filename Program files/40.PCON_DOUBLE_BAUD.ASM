;DOUBLING BAUD RATE
CSEG AT 0
	MOV TMOD, #20H
	MOV TH1, #-3 	; AS USUAL: 9600 bps
	MOV SCON, #50H
	SETB TR1
	MOV A, PCON
	SETB ACC.7  	// MAKING SMOD : 1 
	MOV PCON, A		; 9600 *2 =19200 bps	
	MOV SBUF, #4
	WAIT: JNB TI, WAIT
	CLR TI
END	